#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    double a,b,x;
    cin>>a>>b;
    if (a<=b)
    cout<<"Данные введены некорректно";
    else {
    x=floor(a/b);
    cout<<a-x*b;
    }
    return 0;
}
